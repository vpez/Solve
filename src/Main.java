import java.util.*;
import java.util.function.Function;
import java.util.function.ToIntFunction;
import java.util.stream.Stream;

import static java.util.Comparator.naturalOrder;
import static java.util.stream.Collectors.*;

public class Main {
    public static void main(String[] args) {
//        record MinAndMax(Optional<Integer> min, Optional<Integer> max) {
//        }
//        var numbers = List.of(1, 2, 3, 5, 8, 13, 21, 34);
//        var values = numbers.stream().collect(teeing(minBy(naturalOrder()), maxBy(naturalOrder()), MinAndMax::new));
//        System.out.println(values.min().get() + ", " + values.max().get());
//
//        var summary = numbers.stream().collect(summarizingInt(e -> e));
//        System.out.println(summary.getMin() + "," + summary.getMax() + "," + summary.getAverage() + "," + summary.getCount() + "," + summary.getSum());

//        Deque<Integer> deque = new LinkedList<>();
//        deque.push(1);
//        deque.push(2);
//        deque.push(3);
//
//        while (!deque.isEmpty()) {
//            int i = deque.pop();
//            System.out.println(i);
//        }

//        Collection<Integer> list1 = new ArrayList<>(List.of(1, 2, 3, 4));
//        list1.remove(1);
//        System.out.println(list1);

//        var list2 = new ArrayList<>(List.of(1, 2, 3, 4));
//        list2.remove(1);
//        System.out.println(list2);


    }
}
